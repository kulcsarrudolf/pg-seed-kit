// Public API is assembled across subsequent PRs:
//   - core runner + config (runPendingSeeders, getSeederStatuses, ...)
//   - shared types (Adapter, PgSeederConfig, ...)
// ORM adapters are published under subpaths (e.g. `pg-seed-kit/prisma`).
export {};
